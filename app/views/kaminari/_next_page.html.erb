<% unless current_page.last? -%>
  <%= link_to raw(t 'views.pagination.next'), url, rel: 'next', remote: remote, class: 'pagination-next' %>
<% else %>
  <%= link_to raw(t 'views.pagination.next'), url, rel: 'next', remote: remote, class: 'pagination-next', disabled: true %>
<% end %>
