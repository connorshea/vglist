<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= (yield(:title) + " | " unless yield(:title).blank?).to_s + "Continue From Checkpoint" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', nonce: true, 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <%= link_to("Continue From Checkpoint", root_path, class: "navbar-item has-text-weight-semibold") %>
        <%# Hamburger menu for mobile %>
        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navMenu">
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
        </a>
      </div>

      <div class="navbar-menu is-shadowless" id="navMenu">
        <div class="navbar-start">
          <%= link_to("Games", games_path, class: "navbar-item") %>
          <%= link_to("Releases", releases_path, class: "navbar-item") %>
          <%= link_to("Users", users_path, class: "navbar-item") %>
          <%= link_to("Platforms", platforms_path, class: "navbar-item") %>
          <%= link_to("Genres", genres_path, class: "navbar-item") %>
          <%= link_to("Companies", companies_path, class: "navbar-item") %>
        </div>

        <div class="navbar-end">
          <% if user_signed_in? %>
            <%= link_to(current_user.username, user_path(current_user.id), class: "navbar-item") %>
          <% end %>
          <%= render 'devise/menu/sign_up_items' %>
          <%= render 'devise/menu/sign_in_items' %>
        </div>
      </div>
    </nav>

    <%# Rails flash messages %>
    <% flash.each do |type, message| -%>
      <div class="notification <%= flash_class(type) %>"><%= message %></div>
    <% end %>

    <div class="section">
      <div class="container is-fluid">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
