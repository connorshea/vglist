{
    "scripts": {
        "setup": "./bin/setup-worktree && bundle install && pnpm --dir frontend install",
        "run": "./bin/dev",
        "archive": "./bin/teardown-worktree --yes"
    },
    "runScriptMode": "concurrent"
}
